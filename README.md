# 조리로봇 연기 조기 경보 — 김과 연기 구별

> **1회차 완료. 결론은 `이 설계로는 못 가름`.** 2026-08-14.
> 합성 2,380장(분할 다섯 × 양성 476장)으로 YOLOv8s 를 다섯 번 학습하고 평가했음.
> 판별비 E/D 는 **0.90 \~ 1.54** 이나, 이는 점추정일 뿐임. **서로 다른 세 확인이
> 모두 이 값을 최종 판정으로 읽을 수 없게 만들었음.**
> **성능 수치가 나왔으나 성능으로 읽지 않음.**
>
> 발연 출처 **15개**(뽑음 734장 · 서로 다름 666장 · 씀 333장), 급식실 김 **11곳**
> (2,233장 중 서로 다른 것 2,044장).
> 오려내기를 통과한 것은 **15출처 중 6**, 만든 소재 175장 중 **쓸 수 있는 것 67장**.
> 착수 시점(2026-08-08)에는 발연 출처가 2개였고 **`실험이 성립하지 않음`** 이었음.
>
> 이 저장소는 [kitchen-fire-poc](https://github.com/K-H-MOON/kitchen-fire-poc) 의
> **6·7회차에서 `미해결` 로 종결한 연기 라인을 다시 여는 것**임.

급식실 튀김솥에서 **불이 붙기 전에 올라오는 흰 유증기**를 잡고, 그것을 **조리 중
발생하는 김**과 구별하는 것이 목적임.

화염은 이미 잡힘 — 앞선 저장소 10회차에서 주방 화재 인식률 49.7%, 사건 단위 8/8,
잘라 낸 평가 구간의 시작 프레임 기준 지연 중앙값 0초를 확인했음. 이는 **8개 평가
이벤트 안에서의 결과**이며, 실제 발화 시점부터의 지연·현장 성능·로봇 연동을 뜻하지
않음. **화염이 보일 때는 이미 불이 난 것**이며, 그보다 앞선 신호가 필요해서 연기를 봄.

> **적용 범위.** 이 저장소의 1회차는 단일 프레임 실험임. 실제 급식실의 발연 양성
> 자료, 시간 흐름 모델, 실제 추론 지연 및 로봇 제어 연동은 검증하지 않았음. 따라서
> 급식실에서 연기를 조기 경보할 수 있다는 성능 또는 배포 가능성은 주장하지 않음.

> **저장소 안의 별도 작업.** [`docs/HANDOFF_DFIRE_YOLO11.md`](docs/HANDOFF_DFIRE_YOLO11.md)
> 및 관련 D-Fire/YOLO11 문서는 화재 합성 프록시를 다루는 별도 탐색 기록임. 연기 구별
> 1회차의 후속 실험이나 2회차 결과가 아니며, 본 README의 결론을 바꾸지 않음.

---

## 1회차 결과 — 이 설계로는 김과 연기를 못 가름

    분할   E macro   E t-근사 참고 구간   D macro   D t-근사 참고 구간  판별비   최종 판정
     1      0.650   [ 0.047, 1.254]      0.600   [0.436, 0.763]     1.08    보류 (점추정상 성립)
     2      0.807   [ 0.593, 1.020]      0.898   [0.826, 0.970]     0.90    보류 (점추정상 미달)
     3      0.634   [ 0.167, 1.101]      0.411   [0.249, 0.574]     1.54    보류 (점추정상 성립)
     4      0.901   [ 0.690, 1.113]      0.925   [0.866, 0.984]     0.97    보류 (점추정상 미달)
     5      0.398   [-0.256, 1.051]      0.412   [0.240, 0.583]     0.97    보류 (점추정상 미달)

    폭   E 0.398 \~ 0.901 · D 0.411 \~ 0.925 · 판별비 0.90 \~ 1.54
    conf 0.10 · 출처별 macro · 구간은 출처별 값에 t-근사를 적용한 참고값

**이 표를 성능으로 읽지 않음.** 성립/미달 판정을 못 읽게 만든 확인이 셋이고,
서로 다른 절차인데 같은 곳을 가리킴. 특히 비율에 t-근사를 적용한 구간은 [0, 1] 밖으로
나올 수 있어 정식 95% 신뢰구간으로 해석하지 않음. 다음 실험을 한다면 출처 단위
재표본화 등 구간 산출법과 입력 기하를 사전에 확정해야 함.

### 하나 — 정탐이 연기 위에서 나오지 않음

1층이 `정탐이 냄비 위가 아닌 곳에서 나오는지는 1회차 결과에서 눈으로 확인함` 이라고
적어 두었음. 표본을 90칸으로 늘리고, **사용자와 모델이 서로의 값을 보기 전에 각자 세어**
견줬음. 세기 전에 읽는 기준(0.5 / 0.8)을 못 박았음.

    E 정탐 중 `연기 위` 비율   사용자 0.431 · 모델 0.293   (일치 50/58 = 0.862)
    어긋난 칸을 전부 한쪽으로 몰고 애매까지 몰아도 0.466 -> **0.5 를 못 넘음**

정탐은 벽 · 전광 시계 · 방송 자막 · 검은 배경 · 화구 · 소방차 휠에서 나왔음.
**장 단위 채점이 이 모델에서 뜻을 잃었음.**

### 둘 — 1층이 정하지 않은 입력 기하 한 칸으로 판정이 뒤집힘

1층은 `imgsz 640` 만 정했고 **짧은 변을 어떻게 채울지(letterbox)는 안 정했음.**
같은 자료·같은 가중치를 직사각과 정사각 두 기하로 재 봤음.

    분할        1      2      3      4      5
    직사각    1.08   0.90   1.54   0.97   0.97    성립 {1, 3}
    정사각    0.97   0.79   1.51   1.09   0.97    성립 {3, 4}

**다섯 중 둘이 뒤집혔고 방향이 서로 반대임.** 성립한 분할의 개수는 둘로 같아서,
개수만 보면 아무 일도 없던 것처럼 보임.

### 셋 — 95% 구간으로 보면 다섯 분할 모두 성립선을 걸침

E 구간이 다섯 분할 모두 1 을 넘고 한 분할은 0 아래로도 감. **값이 양극단이기
때문임** — 어떤 출처는 거의 다 잡히고 어떤 출처는 거의 안 잡히며 가운데가 드묾.
같은 출처라도 분할이 다르면 0.000 과 1.000 을 오감.

    출처별 E 값 24개 중  0.05 이하 4개 · 0.95 이상 8개 · 가운데 12개
    보기) 분할 5   p2 0.000 · 07 0.946 · q1 0.042 · j04 1.000 · 05 0.000

1층이 층화를 안 한 이유로 `출처가 달라졌으면 결론이 뒤집혔을 것인가` 를 적어 두었음.
**뒤집힘.**

### 학습 지표는 반대 방향을 가리켰음

    분할   소재   재사용   train mAP50   train mAP50-95   E macro
     1     43   11.07회    0.9950         0.9053         0.650
     2     67    7.10회    0.9933         0.8470         0.807
     3     21   22.67회    0.9948         0.9161         0.634
     4     60    7.93회    0.9945         0.8848         0.901
     5     15   31.73회    0.9950         0.9637         0.398

`val: images/train` 이므로 이 값은 **외운 정도**임. 소재가 적을수록 높고(스피어만
-1.000), **학습을 잘 맞힌 분할일수록 진짜 연기를 못 잡았음**(피어슨 -0.880).
train mAP50 의 폭은 0.0017 인데 평가 E macro 의 폭은 0.503 임.
**학습 지표만 보면 다섯 분할이 같아 보임.**

### 그래서 무엇이 남는가

    남는 것    이 설계의 어디가 비어 있는지가 드러남 — letterbox · 채점 단위 ·
              구간 방법 · 판정을 붙일 수준. 2회차 사전 등록에 넣을 항목이 생김
    안 남는 것  `김과 연기를 가른다` 는 어떤 주장도 못 함. 분할 1·3 의 성립도 그대로
              못 읽음
    안 한 것    성능이 안 나왔다고 해서 자료나 기준을 바꿔 다시 돌리지 않았음.
              사전 등록한 대로 내고 못 읽는 이유를 적었음

---

## 학습 전에 나온 가장 큰 결과 — 단일 프레임으로는 사람도 구별 불가

2026-08-11 에 확인한 것임. **모델을 돌리기 전에 나왔고, 이 실험이 도달할 수 있는
최대치를 정함.**

배경 오탐 후보로 뽑아 둔 급식실 튀김 영상(논현중)의 프레임을 훑던 중,
원본 영상을 재생해 보고 이렇게 판단을 내림.

> 프레임으로 볼 때에는 김이 발생했는지 아닌지 잘 파악이 안 되는 부분이 있어서
> 원본 영상을 재생해 살펴보니, **영상에서는 김이 관찰되나(짙은 김은 아님)
> 단일 프레임인 사진 파일에선 구분하기 힘들다.**

재생에서 김이 보인 구간(3:11\~3:18)의 194초 프레임을 원본 해상도(1920×1080)로
내려받아 솥 위만 잘라 키우니 흰 것이 보임. **그러나 김이 있다고 짚지 않은 200초를
같은 잘라내기 같은 크기로 나란히 놓으니 구별이 되지 않았음.**

![단일 프레임 한계](docs/single_frame_limit.jpg)

    194초   솥 위에 뿌연 것이 있음      재생에서 김이 확인된 구간
    200초   솥 위에 뿌연 것이 있음      김이라고 짚지 않은 구간
                                    ← 둘을 못 가름

**위가 194초, 아래가 200초임.** 같은 영상 · 같은 잘라내기 · 같은 크기 ·
원본 1920×1080 에서 솥 위 공간만 떼어 1,200px 로 키운 것임.

둘 중 하나임. 200초에도 김이 있거나(재생에서 짚은 목록이 전부가 아니거나),
보이는 것이 김이 아니라 스테인리스 결이거나임.
**한 장으로는 어느 쪽인지 정할 수 없음.** 시트 해상도의 문제가 아니었음 —
원본 해상도로 확대해도 못 가름.

### 앞선 화재 저장소에도 같은 항목을 기록해 놨음

> **단일 프레임 접근의 한계 가능성도 열어둠.** 김과 연기는 둘 다 회색 반투명
> 부유물이며 **정지 이미지 한 장으로는 사람도 구별이 어려움.** 결정적 단서는
> 시간에 있다 — 끓는 김은 일정하게 유지되나 과열 연기는 점점 짙어지고 어두워짐.

그리고 종결 판정의 근거 셋 중 셋째가 이것이었음.

> 김과 연기는 정지 화면에서 같은 물체(회색 반투명 부유물).
> 구별 단서는 **시간 변화**에 있을 가능성이 높음

**앞선 저장소는 모델 실험을 통해 이 결론에 도달했고, 이번에는 사람이 눈으로 관찰해
같은 결론에 도달함.** 서로 다른 경로로 같은 자리에 왔으므로 이 관찰의 무게가 큼.

### 무엇이 달라지는가

**모델에 요구할 수 있는 한계가 정해짐.** 이 실험의 모델도 한 장을 봄.
**사람이 한 장에서 구별하지 못하는 프레임을 모델이 구별하기를 기대할 수 없음.**
판별비가 1을 넘지 못해도 그것이 모델의 실패인지 과제 자체의 한계인지 갈라 말해야 하며,
그 구분을 사전 등록에 적음.

**라벨 절차가 바뀜.** 사람 판정은 반드시 **재생**으로 함. 프레임 시트는 자리를 찾는
도구이지 판정 도구가 아님. `시트로 짚고 원본 한 장을 확대해 확정` 하는 절차를
설계했다가 폐기했음 — 한 장으로 안 보이는 것은 확대해도 안 보임.

**배경 음성 자료가 무너짐.** 아래 「배경 오탐군」 절에 적음.

**가치도 있음.** 앞선 저장소는 이 한계를 `모델이 못 하니까` 로 적었음.
이번에는 `사람도 못 하니까` 로 적을 수 있고, 대조 그림으로 보일 수 있음.
**더 강한 근거임.**

---

## 왜 종결했던 라인을 다시 여는가

앞선 저장소에 **`연기 조기 경보 — 미해결 · 종결`** 로 기록돼 있음. 6·7회차 두 번의
통제 실험 모두 조리 연기/수증기 판별비가 1 미달이었음(0.54 → 0.75).

다시 여는 이유는 **그 결론이 틀린 평가군 위에서 났을 가능성** 때문임.

| 그룹 | 구성 | 실제로 무엇인가 |
|---|---|---|
| A-smoke | D-Fire 실연기 150장 | **실외 화재의 회색 연기 기둥** |
| E | 스톡 조리 연기 179장 (영상 14개) | 일반 조리 연기. 유류화재 전조가 아님 |

**둘 다 이 저장소의 목표가 아님.** 목표는 실내 튀김솥 위의 흰 유증기임.

같은 일이 화염 쪽에서 이미 일어났었음. 1\~8회차 내내 A(D-Fire)로 화재 인식률을
쟀는데, 8회차에서 새로 잡힌 이미지를 눈으로 열어 보니 **대부분 산불**이었음.
그래서 주방 화재 전용 평가군 F 를 새로 만들었고, 그때부터 숫자가 의미를 갖기
시작했음 — F 인식률 42.3% → 49.7%, 판별비 5.97 → 25.33.

**화염 쪽은 평가군을 바로잡았으나 연기 쪽은 바로잡지 않은 채로 종결했음.**

### 당시 없던 자료가 지금 있음

평가군 F 를 만들면서 수집한 소방 재현 실험 영상의 **점화 직전 구간**에 주방 유류화재
발연 장면이 들어 있음. 착수 시점에는 이것을 kfire02·03 의 **40장**으로 적었음.

**그 40장은 검증된 숫자가 아니었음.** 앞선 저장소 `docs/PREREGISTER_F.md` 의 **화염없음** 열
(9 + 31)을 그대로 옮긴 값이며, 그 프레임에 연기가 보이는지는 확인한 적이 없었음.
실제로 kfire01 의 화염없음 17장을 한 장씩 열어 보니 웍만 놓인 세팅 장면, 소화기
제품컷, 배추 트레이였고 연기는 거의 없었음. **화염없음과 발연은 다름.**

이후 원본 영상을 되찾아 다시 세었고, 새 자료를 더해 지금은 **발연 출처 15개**임.

김 쪽도 마찬가지임. 당시 수증기 평가군 D 는 스톡 영상 24개였고 **급식실이 아니라는
것을 한계로 적어 뒀음.** 지금은 **급식실 11곳**의 국탕·튀김·볶음 조리 영상을 확인했음.

---

## 김과 연기는 무엇이 다른가

### 눈에 보이는 차이는 생각보다 약함

| | 김 | 연기 |
|---|---|---|
| 색 | 백색 (물방울이 모든 파장을 고르게 산란) | **초기엔 청백색\~흰색**, 타면 회색\~검정 |
| 밀도 | 옅고 뒤가 비침 | 짙으면 뒤를 가림. **초기엔 옅음** |
| 경계 | 흐릿함 | 흐릿함 |

**초기 유증기는 흰색에 가까워 김과 구별되지 않음.**

### 시간으로 보면 다름 — 다만 가정에 반례가 하나 나옴

**김은 물방울이라 상승하면서 다시 증발해 사라짐. 연기는 고체 입자라 증발하지 않고
계속 떠다니며 쌓임.**

    김     발생 → 상승 → 소멸        (제자리에서 순환)
    연기   발생 → 상승 → 확산 → 축적  (공간에 축적)

**한 장짜리 사진에는 이 차이가 들어가지 않음.** 어느 순간을 잘라내도 흰 덩어리가 솥
위에 있는 그림이며, 곧 사라질 김인지 계속 쌓일 연기인지 알 방법이 없음.

**그런데 `김은 일정하게 유지된다` 는 가정에 반례가 하나 생겼음.**
로봇고 튀김(배출) 영상을 재생해 보니 우측 솥의 김이 **초 단위로 났다 사라졌다를
반복**했음. 앞선 저장소는 김을 `일정하게 유지` 로 놓고 연기의 `점점 짙어짐` 과
대비시켰는데, 조건에 따라 김도 일정하지 않음(그쪽은 끓는 국탕, 이쪽은 튀김 배출 뒤의
잔열). 한 자료의 관찰이므로 단정하지 않고 **점검 항목**으로 둠.
**다중 프레임 설계로 갈 때 이 기준이 흔들릴 수 있음.**

### 6·7회차에서 수치로 드러난 것

| 무엇을 무엇으로 쟀나 | 결과 |
|---|---|
| 합성으로 학습 → **합성 검증셋** | mAP50 **0.939** (거의 완벽) |
| 합성으로 학습 → **실사 조리 연기** | 인식률 **8.9%** |

#### mAP50 이 무엇인가

**mean Average Precision at IoU 0.5** 의 줄임말이며 세 조각으로 나뉨.

- **IoU 0.5** — 모델이 그린 박스와 정답 박스가 **절반 이상 겹쳐야** 맞다고 인정함.
  연기는 있는데 박스가 엉뚱한 데 있으면 틀린 것으로 셈
- **AP (Average Precision)** — 신뢰도 임계값을 0부터 1까지 바꿔가며 정밀도-재현율
  곡선을 그리고 그 아래 면적을 잼. **놓치지 않는 것과 헛잡지 않는 것을 동시에** 봄.
  임계값을 0.9 로 두면 확실한 것만 잡아 헛잡는 것은 거의 없으나 많이 놓치고,
  0.1 로 낮추면 웬만한 것은 다 잡으나 김까지 함께 잡음. **AP 는 그 사이 모든 지점의
  성적을 하나로 요약한 값이며 임계값 한 지점의 점수가 아님**
- **mean** — 클래스별로 구한 AP 를 평균함. **다만 0.939 는 평균값이 아니라 smoke
  클래스 단독 값임** (앞선 저장소 `docs/ROUNDS.md` 기준)

그러니 0.939 는 **`합성 검증셋에서 위치까지 맞춰가며 거의 완벽하게 잡았음`** 이라는
뜻이며 **`93.9% 확률로 인식`이 아님.**

#### 두 값을 직접 빼면 안 되는 이유

| | 세는 단위 | 위치 조건 | 헛잡는 것 |
|---|---|---|---|
| mAP50 | 박스 | 50% 이상 겹쳐야 인정 | 함께 봄 |
| 인식률 (이 저장소) | 프레임 | 없음. 박스 하나만 나오면 성공 | 따로 봄 (오탐률) |

임계값 처리도 다름 — mAP50 은 전 구간을 요약하고, 앞선 저장소의 인식률은
**conf 0.25 한 지점**의 값임. 기준이 다르므로
**지표 정의만으로 생기는 차이가 얼마인지 알 수 없음.**

**그러나 방향은 분명함** — 합성 연기의 질감은 외웠으나 실제 장면에서 그 질감이 김과
구별되지 않았음. 지표 차이로 설명될 크기가 아니며 도메인 전이 실패임.

### 발생 조건의 비대칭

**연기는 이상 상태임.** 기름 온도가 임계를 넘거나 음식이 탈 때만 발생함.
화재는 드물게 일어나는데 김은 자주 발생하므로, **김을 연기로 오인하면 경보가
계속 울림.**

> **정정.** 이 저장소에 `김은 급식실에서 상시 발생함` 이라고 적었으나 **틀린 문장이었음.**
> 급식실 28편을 전수로 보니 조리 종류·조리 단계·재료의 수분에 따라 크게 달랐음.
> 볶음만 하는 시간대나 배출 시점에는 김이 거의 없음. 자세한 것은
> [자료원 기록](docs/SOURCES.md) 의 「김의 양을 정하는 세 축」.

---

## 목표를 어디에 둘 것인가 — 흰 연기

| 온도 | 단계 | 연기 색 |
|---|---|---|
| 200\~230도 | 발연점 | **흰색\~청백색** |
| 300\~330도 | 인화점 | 흰색 (불씨가 닿으면 붙는 상태) |
| 360\~380도 | 발화점 | 저절로 발화 → 이때부터 검은 연기 |

**검은 연기는 이미 늦은 신호임.** 회색이나 검은 연기가 보인다면 기름이 타고 있다는
뜻이고 그 시점에는 화염도 함께 보임. **화염은 10회차 모델이 이미 잡음.**

| 목표 | 구별 난이도 | 얻는 것 |
|---|---|---|
| 검은 연기 | 쉬움 | 거의 없음. 이미 화염 탐지가 덮는 구간 |
| **흰 연기** | **어려움** | **발화 전 수십 초\~수 분** |

정확한 목표를 한 문장으로 요약하면 — **튀김솥 위에서 발화 직전에 올라오는 흰 연기.**

---

## 시각이 아닌 다른 판단 근거 — 발생 위치 (그러나 약함)

**김과 연기가 발생하는 위치가 다름.** 국탕솥은 물이라 김이 정상이고, 튀김솥은
기름만 있으니 흰 것이 이상 신호라는 논리임. 앞선 저장소의
[탐지 JSON 규격 5단계](https://github.com/K-H-MOON/kitchen-fire-poc/blob/main/docs/DETECT_SCHEMA.md)
에 **화구 영역 판정이 이미 만들어져 있음.**

**급식실 자료를 확인하고 나서 약해진 이유는 이러함.**

| 화구 | 내용물 | 흰 것이 오를 때 |
|---|---|---|
| 국탕솥 | 물 | 김. 위치로 가려짐 |
| **튀김솥** | 기름 + 젖은 재료 | **정상 유증기와 화재 전조가 같은 자리에서 남** |

**남일고 튀김 조리 영상이 그 근거임** — 정상 조리인데 유증기가 화면을 뒤덮고,
목표와 **같은 자리 같은 조리기구**에서 남. 발생 위치는 국탕솥과 튀김솥을 가르는
데까지만 유효하며, **튀김솥 안에서 정상과 이상을 가르는 일은 그대로 남음.**

**화구가 여럿인 프레임을 따로 확보했음.** 숭곡중 튀김은 튀김기 둘의 투입 시점이
다르고, 로봇고 튀김은 `0\~13초 우측 솥만` `14\~33초 정면만` 으로 시간대가 겹치지 않음.
진선여고는 **목표 화구가 조용하고 뒤쪽에서만 김이 나는** 영상까지 짝으로 있음.
모델이 `화면에 김이 있다` 가 아니라 **어느 화구에서 났는지**를 가리는지 잴 수 있음.

---

## 확보한 자료

    발연 (양성)   출처 15개 · 초당 2장으로 뽑음 734장 · 서로 다름 666장 · 씀 333장
    김 (음성)     급식실 11곳 · 2,233장 · 서로 다른 것 2,044장
    배경 (음성)   급식실 정상 조리 · 서로 다른 것 207장 (개원중 178 · 로봇고 29)
    연기 소재     위 발연 15출처 중 **여섯**을 오려내 175장 → 쓸 수 있는 것 **67장**

**발연 장수가 982 → 734 로 줄었음.** 구간을 다시 확정하며 자막 화면·야외 컷·화염이
섞인 토막을 뺐고, 앞선 표가 `Σ(끝−시작)` 이 아니라 토막마다 +1 을 더하고 있었음.
`씀 333장` 은 거기에 출처당 상한 32장을 적용한 값임.

**출처별 표와 판정 경위는 상세 문서에 있음.** 이 문서에는 결론만 적음 —
표를 두 곳에 두면 한쪽만 고쳤을 때 어긋나기 때문임. 실제로 그 일이 한 번 있었음.

→ 발연 15개: **[자료원 기록](docs/SOURCES.md)** · 구간: **[발연 구간 확정 기록](docs/TIMELINE.md)**
→ 김 11곳: **[김 구간 기록](docs/STEAM_TIMELINE.md)**

### 발연 표를 통째로 다시 작성했음

처음 표는 출처마다 시간 단위가 달랐음 — 전부 재생 초로 적어 놓고 순위를 매겼는데
실제로는 배속·컷·슬로우모션이 섞여 있었고, `끊기지 않는 연속 길이가 가장 중요한 값`
이라고 적어 놓고 정작 **토막의 합계**를 적었음. 열다섯 개를 원본 해상도로 다시 보고
고친 결과 **처음 표에서 순위가 맞은 것은 최장 출처 하나뿐이었음.**

지금은 **최장 토막** 순으로 매기고, 장수는 예상값이 아니라 `manifest.json` 에서 읽은
측정값을 씀. 가장 긴 것이 171초 한 토막(341장 뽑아 상한 32장 씀)이고,
가장 짧은 것이 6초 다섯 토막임.

### 출처당 최소 장수 — 하한선 10장

평가군은 **출처별 macro 평균**을 씀. 출처마다 점수를 따로 내고 그 평균을 쓰는 방식임.
그러면 장수가 적은 출처 하나가 평균을 크게 흔듦 — 2장짜리 출처는 한 장만 틀려도
그 출처 점수가 50%가 됨. 그래서 **출처당 최소 10장**을 하한선으로 정했음.
앞선 저장소 평가군 F 에서 kfire05 가 화염없음 2장뿐이라 macro 계산이 망가진 전례가
있어 규칙으로 굳힌 값임.

**출처 하나가 이 하한선과 정확히 같음.**
`Let's Chat — Cooking Fire Prevention` 은 8초를 초당 2장으로 뽑아 15장이 나오는데
**슬로우모션이라 서로 다른 것이 10장뿐임.**
**한 장이라도 적었으면 이 출처를 빼고 배정을 다시 짜야 했음.**
설계가 막아 준 것이 아니라 운이 좋았던 것이며, 하한선에 걸리는 출처가 있을 수 있다는
것을 **추출 전에** 확인했어야 함.

### 김 자료는 급식실 11곳 전부 실제 현장임

대형 국솥, 튀김솥, 회전솥, 오토틸팅, 후드, 스테인리스 조리대, 조리사가 그대로 나옴.
배경이 밝은 스테인리스와 흰 타일이라 **흰 김과 대비가 약함** — 이번 실험이 실제로
마주할 조건임. 드라이브의 급식실 조리 영상 **28편을 전수로 보고** 판정했으며,
처음 이 저장소에는 **4곳**으로 적혀 있었고 시설 이름도 `성곡중` `남일중` `부산체육중`
으로 잘못 적혀 있었음(`숭곡중` `남일고` `부산체고` 가 맞음).

**태그를 세 갈래로 나눔.** `투입`(재료를 넣은 직후 · 화면을 뒤덮음 · 초당 4장),
`근접`(앞쪽 화구 · 초당 2장), `원거리`(뒤쪽 화구이거나 카메라가 멂 · 초당 2장).
**원거리는 세기가 아니라 크기의 구별임** — 둘을 섞으면 점수를 잘못 읽음. 원거리 김은
모델이 못 보고 지나가 오탐률이 낮게 나올 수 있는데 그건 잘 구별한 것이 아니라
못 본 것임.

### 밝기가 한쪽으로 쏠려 있음

발연 자료는 어두움 8 · 밝음 4 · 특수 2 로 섞여 있는데 **김 자료는 밝은 쪽으로
몰려 있음.** 어두운 급식실로 기록해 둔 울산현대차도 프레임을 실제로 재 보니
평균 휘도 81\~94 로 어둡지 않았고, 화면에서 어두운 것은 급식실이 아니라
**스테인리스 회전솥 안쪽 그늘**이었음.

결과를 볼 때 **밝기 때문인지 김과 연기의 차이 때문인지 갈라낼 수 없음.**
발연의 `밝은 배경 4개` 와 함께 이 실험의 두 맹점임.

---

## 학습·평가 배정 — 프레임을 한 장도 보기 전에 확정함

**층화 없이 시드 고정 단순 무작위. 학습 10 : 평가 5. 분할 다섯.**
`scripts/assign_split.py` 와 `scripts/split_s1.json` 에 있음.

**왜 층화를 안 하는가** — 1회차에서 재려는 값이 **출처가 달라졌으면 결론이 뒤집혔을
것인가** 이기 때문임. 층화는 층 구성이 흔들리는 몫을 배정에서 제거하므로
재려던 것을 못 재게 됨. **분할 간 폭이 재려던 것임.**

| 시드 | 평가 출처 | 평가군(서로 다름) | 학습 소재 |
|---|---|---:|---:|
| 1 | ft · j01 · p2 · q1 · j12 | 95 | 255 |
| 2 | s2 · ft · 05 · kfire02 · 04 | **417** | 242 |
| 3 | j01 · m3 · 07 · kfire03 · q1 | 158 | 217 |
| 4 | ft · m3 · p2 · kt · 05 | 121 | 245 |
| 5 | p2 · 07 · q1 · j04 · 05 | 109 | 250 |

**평가군 크기가 최대 4.4배 벌어짐.** 시드 2 는 s2 하나가 평가군의 82% 를 차지하고,
시드 5 는 옅음 프레임이 0장이라 옅음 성능을 아예 못 잼. 출처 macro 를 쓰기로 한 것이
이 차이를 완화하나, **분할마다 재는 것이 달라진다는 사실 자체는 macro 로 없앨 수 없음.**

**12번(Deep-Frying)은 다섯 분할 어디에서도 평가군에 들어가지 않음.**
학습에만 쓰이고 성능이 한 번도 채점되지 않음. 한 분할에서 평가에 안 걸릴 확률이
2/3 이고 다섯 번 다 안 걸릴 확률이 **13.2%** 이므로 이상한 일이 아님.
배정은 유효하며 사실로 적어 둠.

---

## 채점 기준

### 정탐 판정 단위 — 장 단위. 고른 것이 아니라 자료가 정함

한 장에 임계값 이상인 상자가 하나라도 있으면 정탐으로 셈. **박스와 IoU 는 안 씀.**

박스가 흔들리는 것을 재고 정했음. j01 45.0초(발화 직전 · 가장 뚜렷한 축)에
두 가지 다 말이 되는 읽기로 상자를 그려 봄.

    좁게  확실히 짙은 데까지만    54,600px²
    넓게  옅게 이어지는 위쪽까지  247,500px²   넓이비 4.5배
    IoU = 0.221                            관문 0.5 → 탈락

**같은 프레임 같은 연기인데 두 읽기가 IoU 0.22 임.** 원인은 옅고 짙고가 아니라
**위쪽 경계가 없다는 것** — 연기 기둥이 올라가며 벽 색으로 녹아듦. 아래(냄비 테두리)와
좌우는 정해지는데 위가 안 정해짐. 730\~928장을 그려도 그 상자들이 서로 IoU 0.22 로
갈리면 **재는 것이 연기가 아니라 그리는 사람임.**

**장 단위의 맹점은 그대로 남음** — 어디를 봤는지 모름. 뒤쪽 국솥에 반응해도 정탐이 됨.
모델이 어디에 반응하는지를 봐야 아는 값이므로 **1회차 결과에서 눈으로 확인하는
항목**으로 사전 등록에 적음.

### 지표 — E·D 를 따로 보고하고 판별비를 함께 냄

운용 기준선 한 점에서 **E(조리 연기 인식) · D(김 오탐) · 판별비 E/D** 셋을 다 적고,
임계값을 훑은 곡선은 그림으로 붙임.

**판별비 하나만 보면 안 되는 이유가 앞선 저장소에 실증으로 있음.** 7회차에서 판별비가
0.54 → 0.75 로 올랐으나 원문 그대로 **`올라야 할 E는 11.7%→8.9%로 내려갔`** 음.
분모가 더 빨리 줄어 비율만 오른 것임. 그래서 그 저장소에 **`두 지표를 항상 함께 봄`**
이 못 박혀 있음.

### 임계값 — 운용 기준선 conf 0.10 · 네 점을 함께 실음

`conf 0.03 · 0.10 · 0.25 · 0.40` 을 다 재고 **0.10 한 점으로 판정함.**

앞선 저장소가 같은 값을 씀. 그쪽은 이 값을 **자기 모델의 곡선을 보고** 정했음 —
`B 가 0.10\~0.20 에서 2.7% 로 평평 → 그 구간 시작점이 오탐 최소 상태의 최고 인식률`.
1\~4회차는 0.25 였고 5회차에 바꾼 것임.

**그러니 우리가 1회차에 0.10 을 쓰는 것은 남의 5회차 값을 빌려 오는 것임.**
우리 모델의 곡선은 다른 자리에 꺾일 수 있음. 그래도 빌려 오는 까닭은,
기준선을 우리 결과를 보고 고르면 **1회차 판정이 1회차 결과에 기대는 순환**이 되기
때문임. 대신 네 점을 다 실어 `기준선을 잘못 잡아 나온 결론` 이라는 반문을 막음.

**2회차 기준선은 1회차 곡선에서 정함. 이것을 지금 적어 둠.**
앞선 저장소가 5회차에 한 것과 같은 일을 1회차와 2회차 사이에 두는 것임.
미리 적어 두었으므로 그때 바꾸는 것은 사후 조정이 아님. 바꾸더라도
**0.10 에서의 값을 함께 실어** 회차 간 비교가 끊기지 않게 함.

### 성립선 — 판별비 1. 그 아래는 등급을 나누지 않음

**판별비 E/D 가 1 이상이면 성립, 미만이면 미달.** 두 단계뿐임.

1 은 우리가 그은 선이 아니라 **뜻이 먼저 있는 자리**임 — 잡아야 할 것에 반응하는
빈도와 무시해야 할 것에 반응하는 빈도가 같아지는 지점이고, 그 아래면
앞선 저장소 표현대로 `잡아야 할 것보다 무시할 것에 더 반응한다` 는 뜻임.
그 저장소는 연기 라인을 종결할 때도, 10회차에 화염을 채택할 때도 같은 선을 썼음.
**우리는 그 종결된 라인을 다시 여는 것이므로 같은 선을 넘느냐가 곧 답임.**

**부분 성립 칸을 두지 않음.** `0.75 를 넘으면 부분` 이 자연스러워 보이나 그쪽 0.75 는
**다른 평가군에서 나온 값**임 — 그쪽 D 는 스톡 클로즈업이고 우리 D 는 급식실에서
직접 찍은 김임. 견줄 수 있는 것은 1 이라는 뜻뿐이고, 그 위에 선을 하나 더 그으면
기준을 억지로 만들어 메우는 것이 됨.

**남는 약점 하나를 적어 둠.** 판별비는 비율이라 **분모가 줄어도 오름.**
7회차가 그랬음 — 0.54 → 0.75 로 올랐으나 올라야 할 E 가 11.7% → 8.9% 로 내려갔음.
E·D 를 항상 함께 적으므로 값은 드러나지만, **`성립` 이라는 판정 자체는 판별비
하나에만 붙음.** 이걸 막으려면 `E ≥ 얼마` 를 같이 걸어야 하는데 **그 얼마의 근거가
없음** — 그쪽 11.7% 는 평가군이 달라 못 빌려 옴. 근거 없는 숫자를 만들어 넣는 대신
**한계로 남기고 판정문에 E·D 를 반드시 병기함.** 규칙이 아니라 서술이므로
규칙만큼 단단하지 않음.

### 분할 합치기 — 다섯 값을 다 적고 폭을 주 결과로 냄

평균·중앙값·최소값을 쓰지 않음. 폭이 애초에 재려던 것이기 때문임.

---

## 평가군의 배경 지름길과 배경 오탐군

### 배경 지름길

    평가 양성 (연기)   전부 소방 시연장 · 실험실 · 야외
    평가 음성 (김)     전부 급식실

이대로 채점하면 **모델이 `여기가 급식실인가 아닌가` 만 봐도 만점이 나옴.**
연기를 배운 것이 아니라 배경을 외운 것인데 점수로는 구분되지 않음.

**방지할 방법은 이미 모은 자료 안에 있음.** 소방 재현 영상에는 물을 붓거나 젖은 재료를
넣은 뒤 오르는 **수증기**가 들어 있음. 수집 과정에서 `화재 뒤의 흰 것은 물부터
의심하라` 며 양성에서 걸러 냈던 것들인데, 버릴 것이 아니라 **음성으로 쓸 것**이었음.
같은 배경에서 김과 연기가 둘 다 나오므로 배경으로는 가릴 수 없게 됨.

완전히 없애지는 못함. 양성 쪽에 급식실이 하나도 없다는 비대칭은 남으며 한계로 유지함.

### 배경 오탐군이 무너짐 (2026-08-11)

`김도 연기도 없는 정상 급식실 조리` 프레임을 따로 두어 **배치 환경에서 헛울리지
않는가** 를 재려 했음. 앞선 저장소의 평가군 B(개원중 CCTV 75장)와 같은 역할임.

후보 1,126장을 뽑아 놓고 확인하다 위의 **단일 프레임 한계**에 부딪혔음.
**한 장으로는 `김이 없다` 를 확정할 수 없음.** 그래서 재생으로 확인했고,
**옅은 김의 기준은 만들지 않기로 함** — 김이 초 단위로 났다 사라졌다 하므로
초당 1장으로 뽑은 그 한 장이 김이 있는 순간인지 없는 순간인지가 **우연**이기 때문임.

**영상 단위로만 가름.** 한 번이라도 김이 확인되면 그 영상은 배경 오탐군에 안 씀.

| 출처 | 뽑음 | 서로 다름 | 판정 |
|---|---:|---:|---|
| 개원중 CCTV 투입 | 289 | 87 | 남음 |
| 개원중 CCTV 배출 | 241 | 91 | 남음 |
| 로봇고 튀김(쉐이크) | 44 | 29 | 남음 |
| 논현중 튀김(full) | 435 | 409 | **뺌** — 재생에서 김 확인 |
| 로봇고 튀김(배출) | 46 | 33 | **뺌** — 우측 솥에 깜빡이는 김 |
| 숭곡중 볶음(재료투입2) | 71 | 68 | **뺌** — 교반기에서 김 |

**서로 다름 717장에서 207장으로, 71% 를 잃었음. 그리고 남은 것의 86% 가
개원중 CCTV 임.**

### 개원중 CCTV — 카메라가 김을 못 담음

튀김솥에 재료를 투입했는데도 김으로 볼 만한 것이 화면에 전혀 없음. 기름이 부글거리는
것은 보이는데 김만 안 보이고 프레임이 뚝뚝 끊어짐. **뽑은 530장 중 서로 다른 것이
178장뿐(66% 감소)** 이며, 다른 급식실은 0\~23% 임. **같은 그림이 반복된다는 뜻임.**

CCTV 는 낮은 비트레이트로 압축되는데 **압축에서 가장 먼저 사라지는 것이 옅고
경계가 흐린 것**임. 흰 김이 정확히 그러함. **그렇다면 연기도 안 보일 가능성이 큼.**

**같은 영상에서 사람은 보였음** — 사람 인식 저장소가 이 CCTV 로 240프레임 중 65장에
사람 라벨을 붙였고 잘 보였음. **작업 범위 사람 인식은 CCTV 로 되지만 연기 조기 경보는
안 될 수 있다는 뜻임.**

**이 자료를 김 자료에서 뺀 근거이기도 함.** 사람이 재생해서 봐도 김이 없으므로
`김이 안 나는 조리` 인지 `카메라가 못 담는 것` 인지 가릴 수 없음.
투입과 배출 두 편이 같은 결과인 것이 소득임 — 한 편만 그랬다면 촬영 시점이 우연히
김이 안 나는 때였을 수도 있으나, **투입은 김이 가장 강한 순간**이고 다른 급식실에서는
같은 순간에 화면을 뒤덮었음.

---

## 이번에 다르게 할 것

### 평가군을 목표 도메인으로 바꿈

**공개 데이터셋으로 검증하지 않음.** D-Fire 의 smoke 는 실외 화재의 회색 기둥이고
이 저장소의 목표는 실내 튀김솥 위 흰 유증기임. 같은 `연기` 라는 이름을 쓸 뿐 다른
물체임. 공개 데이터셋은 **판정 기준이 아니라 참고 지표**로만 씀.

### 합성은 그대로 실행함

급식실에 실제로 불을 낼 수 없듯 **급식실 튀김솥에서 유증기를 낼 수도 없음.**
소방 재현 영상은 급식실이 아니라 실험장이나 야외임. 그대로 학습시키면 모델이
**`그 실험장 배경이면 연기`** 를 외울 수 있음.

    화재    실주방 프레임 + 실화염 소재   → 합성 → 자동 라벨
    연기    실주방 프레임 + 실유증기 소재 → 합성 → 자동 라벨

**합성 배경은 급식실을 쓰되 시설을 완전히 분리함** — 합성에 쓴 급식실이 평가에도
나오면 배경을 외운 것을 성능으로 읽게 됨.

> **2026-08-13 정정 — 이 분리를 못 지켰음.** 김이 없는 급식실 프레임이 개원중 178 ·
> 로봇고 29 둘뿐이고 **둘 다 평가군(배경 오탐군)에 들어 있음.** 조리로봇 영상 열 편을
> 더 훑었으나 열 편 다 화각이 부적합이었음. 개원중을 배경으로 골라 **오염을 성립
> 판정에 안 쓰는 군으로 옮겼을 뿐 없애지 못했음.** 아래 「소재를 만들고 시험 합성을
> 네 번 돌림」과 `docs/PREREGISTER_S1.md` 2층의 「못 지킨 것」에 적음.

### 연기는 화염보다 어려움

**알파 추출이 까다로움.** 화염은 밝기를 기준으로 배경을 지워 오려낼 수 있었음
(휘도 키잉). 연기는 배경보다 특별히 밝지 않고 반투명해서 이 방법이 통하지 않음.
**흰 연기를 밝은 배경에서 오려내면 밝기 차이가 거의 없어 경계가 뭉개짐.**
神戸市消防局 영상이 그 실제 예시임 — 360p 라서 안 보이는 줄 알고 720p 를 다시 받았는데
**진짜 문제는 배경이 은박이라 사람 눈으로도 경계가 안 보이는 것**이었음.

### 실사를 소수 섞으려던 계획 — 철회함

앞선 저장소는 **수작업 바운딩박스 0건**이 원칙이었고, 이번에는 실사 발연 프레임에
손으로 박스를 그려 소수 섞는 것을 검토했음. `연기 박스는 큰 덩어리 하나라 몇 초면
그림` 이라고 적어 뒀음.

**철회함.** 위 「채점 기준」의 IoU 0.221 이 근거임 — 같은 프레임을 두 가지로 읽으면
박스가 그만큼 갈림. **박스를 학습에 넣으면 그 흔들림을 그대로 가르치게 됨.**

---

## 소재를 만들고 시험 합성을 네 번 돌림 (2026-08-12\~13)

### 오려내기 — 15출처 중 여섯만 통과

배경 차분(`알파 = (본 것 − 판) / (255 − 판)`, 세 채널 중 최솟값, 0.06 미만은 0)으로
**여덟 번 시험해서** 정했음. `됨` 이 다섯 → 일곱 → 다섯 → 여섯으로 오갔고,
**두 번은 제가 시험 변수를 빠뜨려 잘못 늘렸던 것**임.

| 통과한 여섯 | 구간 | 잘라내기 |
|---|---|---|
| m3 | 59\~92 | — |
| kfire03 | 24\~36 | — |
| q1 | 37\~48.5 | 위 10% (자막 띠) |
| j04 | 18\~28 | 아래 42% (냄비·버너 화염) |
| 07 | 55\~61.5 · 72\~77 | — |
| p2 | 41\~46.5 · 47\~51 | 위 12% (자막) |

**못 쓰는 아홉의 사유는 셋임** — 판에 이미 연기가 있어 차분할 것이 없음(j01·s2 후반·04),
**밝은 배경이라 연기가 배경보다 안 밝음**(j12 은박·05 밝은 타일·ft 콘크리트),
자막·그래픽·사람이 알파에 들어옴(kfire02·kt·12).

소재 **175장**이 나왔음.

### 소재 175장 중 쓸 수 있는 것 67장

**합성한 장마다 라벨이 참인지 확인함** — 상자 안 화면 변화량이 개원중 프레임이 스스로
만드는 변화(잡음 바닥)보다 작으면 그 장을 안 씀. 바닥은 상자 크기에 따라
2.85\~5.41 계조로 달라지므로 크기마다 다른 바닥을 씀.

    m3       66장 → **0장**
    kfire03  24 → 15 · q1 23 → 17 · j04 20 → 14 · 07 23 → 14 · p2 19 → 7
    합       175 → **67장**

**m3 66장이 통째로 빠졌음.** 상자 넓이비는 다른 출처의 1.56배인데 상자 안 알파는
0.38배로, **희석이 아니라 정말 옅은 것**이었음. 그림 여덟 장 어디에도 김 줄기가 없었음.
문턱을 잡음 바닥의 1사분까지 낮춰도 5장뿐이었음.

**m3 는 오려내기 여덟 번의 시험을 통과해 소재 여섯에 들었음.** 그 통과는 시트를 눈으로
본 것이었고 **배경에 얹었을 때 보이는지는 안 봤음.**

### 시험 합성을 네 번 돌리며 결함 열넷을 잡음

| 회차 | 무엇이 틀렸나 |
|---|---|
| 1 | **순환** — 문턱으로 표본을 나눠 놓고 그 문턱을 검증함 |
| 1 | **합성식** — 오려내기는 연기색을 255 로 놓고 합성은 관측색으로 얹었음 |
| 2 | 잡음 바닥이 **화소 중앙값**인데 견주는 값은 **상자 평균** — 다른 저울 |
| 2 | 검산 셋(`자기 차 0`·`판 되살림`·`알파0 포함`)이 **전부 실패 불가**였음 |
| 2 | 검산 표본이 **전부 m3** 였음(m3 가 66장이라 앞을 다 채움) |
| 2 | `알파0` 은 규칙이 아니라 **이미 한 자르기** — 판정 분기가 죽은 코드였음 |
| 2 | 배경을 178 이 아닌 **30장**으로 씀. 선언한 정의를 코드가 안 지켰음 |
| 2 | **84.1 계조**를 실제 어두워진 양처럼 보고했음. 실제는 **16.6** |
| 3 | 제외 판정이 **크기에 51.6% 흔들림** — 축소가 잡음을 지우기 때문 |
| 3 | 크기를 정하기 전에 제외를 판정함. **순서가 거꾸로였음** |
| 4 | `변화 ≥ 잡음 바닥` 을 **충분조건처럼 썼음.** 필요조건일 뿐임 |
| 4 | 그 탓에 m3 51장이 통과했고 **그림에는 아무것도 없었음** |
| — | `R ≤ 1.2` 를 제안했다가 **이미 가진 자료가 반증**해 쓰기 전에 물림 |
| — | 시트가 화면 전체를 줄여 그려 **223화소 상자를 60화소로 보고 판정**하려 했음 |

**넷 다 학습을 돌리기 전에 잡았고, 결과를 보고 기준을 바꾼 것은 없음.**
다만 **추천을 세 번 물렸고**(`R ≤ 1.2` · `원본에서 판정` · `소재당 한 장`)
그 자체가 이 결정들의 신뢰도를 낮춤.

### 앞선 저장소 원문을 늦게 읽어 셋을 놓쳤음

`kitchen-fire-poc` 의 `web3d/PREREGISTER_SMOKE.md` 를 **네 번째 시험이 끝난 뒤에야**
읽었음. 거기 이미 있던 것 셋.

    **가시성 검사**   `합성 후 박스 영역의 화면 변화량을 재서 보이지 않는 연기는
                    장면째 버린다` — 제가 세 번 고쳐 도달한 그 규칙임
    **배경 적응 틴트** `흰 연기는 흰 급식실에서 보이지 않아 라벨만 남고 신호가 없는
                    학습 예제가 된다`
    **소재 197종**   우리는 67장. 그쪽 사전 등록이 `A-smoke 가 낮게 나오면 첫 번째
                    대응은 알고리즘이 아니라 **소재 확대**다` 라고 미리 적어 두었음

**원문을 먼저 읽었으면 시험 두 번은 안 돌려도 됐음.**

### 틴트를 재 봤더니 우리 배경에서는 세 장어치였음

같은 뽑기에서 색만 갈아 여섯을 쟀음.

| 연기색 | 쓸 수 있는 소재 |
|---|---:|
| **흰색 255 (지금)** | **68** |
| 고정 180 | 42 |
| 검댕 80 | 28 |
| 배경대비 60 | 38 |
| 밝으면검댕 80 | 68 |
| 밝으면검댕 40 | 71 |

**어둡게 가는 쪽은 전부 크게 나빠짐.** 가장 좋은 것도 세 장(4.4%) 늘 뿐임.
개원중 배경 밝기가 대략 150 근처라 흰색 대비(105)가 검댕 대비(70)보다 크기 때문임.

그리고 **그림이 숫자를 뒤집었음** — 검댕으로 얹으면 연기가 **어두운 얼룩**으로 보이고,
연기가 없는 대조군(m3)에서도 **검은 자국이 눈에 보이게** 됨.
**흰색 255 를 유지함.**

**저장소 간 조치를 그대로 옮기면 안 되고 우리 자료에서 재 봐야 한다는 것**을
이 측정이 보임. 제가 원문을 읽고 `흰색식은 알려진 실패 모드입니다` 라고 **재기 전에
단정했던 것**을 이 숫자가 정정함.

### 2층에서 정한 것

`docs/PREREGISTER_S1.md` 의 2층 절에 239줄로 적었음. 요약.

    합성식    배경 × (1 − 알파) + **255** × 알파      틴트 안 씀
    배경      개원중 해밍 > 0 서로 다름 **178장**
    라벨 상자  **질량99** (다섯 후보를 미리 못 박은 기준으로)
    소재      175 → **67장** (피복률 상한 60% + 장마다 가시성 검사)
    위치      무작위 · 크기 가로폭 0.30 \~ 배율 1.0 로그 균등 · 개수 한 장에 하나
    장수      분할마다 **양성 476장** (소재당 재사용을 앞선 저장소와 같게)
    학습 음성  **0장**

**못 지킨 것 셋을 2층에 따로 적었음** — 배경 오탐군을 배경으로 쓴 것,
학습 음성을 한 장도 안 넣은 것, 그리고 **1층이 `수증기를 음성으로 씀` 이라 두 번
적어 놓고 평가군 표에 그 군을 안 넣은 것**(1층은 수정 금지라 우리가 못 고침).

---

## 진행 상황

**완료**

- ✅ 문제 정의 · 6·7회차 재검토 · 목표 확정(발화 직전의 흰 연기)
- ✅ **기존 자료 재검증** — `발연 40장` 이 화염없음 열을 옮긴 값이었음을 확인하고 정정
- ✅ **발연 영상 수집 — 출처 15개** (착수 시점 2개)
- ✅ **급식실 김 자료 확인 — 11곳** (처음 기록은 4곳)
- ✅ **발연 구간 전수 확정** — 15편을 원본 해상도·0.2초 단위로 → `docs/TIMELINE.md`
- ✅ **학습·평가 배정 확정** — 프레임을 한 장도 보기 전에
- ✅ **프레임 추출** — 구간 정정 뒤 재실행. 발연 뽑음 734장 · 서로 다름 666 · 씀 333장
- ✅ **채점 기준 전부 확정** — 장 단위 · E·D 따로 + 판별비 · conf 0.10 · 성립선 1
- ✅ **단일 프레임 한계 확인** — 사람도 한 장으로는 못 가름
- ✅ **배경 오탐군 판정** — 개원중 178 · 로봇고 29. **성립 판정에는 안 씀**
- ✅ **사전 등록 1층** `docs/PREREGISTER_S1.md` — 소재를 한 장도 만들기 전에 닫음
- ✅ **연기 알파 추출** — 15출처 중 여섯 통과 · 소재 **175장**
- ✅ **시험 합성 네 번** — 결함 열넷을 잡음. 소재 175 → **67장**
- ✅ **사전 등록 2층** — 합성식·배경·상자·크기·위치·개수·장수·음성 확정
- ✅ **본 합성** — 분할 다섯 × 양성 476장 = **2,380장** (205초 · 1.29 GB · 버린 자리 0)
- ✅ **합성 QC** — 문턱에 걸친 장이 15.5%. 결과 전에 원인 후보를 적어 둠
- ✅ **1회차 학습 다섯 분할** — YOLOv8s · 60에폭 · imgsz 640 · 배치 16 (합 1.7시간)
- ✅ **1회차 평가** — conf 0.10 · 출처별 macro · 판별비 0.90 \~ 1.54
- ✅ **정탐 위치 확인** — 1층이 요구한 마지막 줄. 두 사람이 90칸을 따로 세어 견줌
- ✅ **입력 기하 민감도** — 직사각 대 정사각. 다섯 중 둘의 판정이 뒤집힘
- ✅ **1회차 판정** — **`이 설계로는 못 가름`.** 성능 수치를 성능으로 안 읽음

**미착수**
- ⬜ **2회차 사전 등록** — letterbox · 채점 단위 · 구간 방법 · 판정을 붙일 수준을
  항목으로 세울 것. 1회차가 그 네 칸이 비어 있으면 어떻게 되는지를 보였음
- ⬜ 김 세기 분류 (짙음/옅음)
- ⬜ 화구 영역 판정과의 결합
- ⬜ **소재 확대** — 자막 잘라내기(kfire02·kt·12) · 판 다시 잡기(s2 전반) · 새 영상.
  **전부 1층 표를 바꾸는 일이라 3회차 이후임**(2회차 단일 변수는 김 negative 로 못 박힘)

---

## 원칙

앞선 두 저장소의 원칙에 이번에 겪은 것을 더함.

**판정 기준을 결과보다 먼저 확정** — 통과선·해석 규칙·예상되는 한계를 사전 등록에 적고
채점 후 수정하지 않음.

**목표 도메인으로 평가** — 공개 데이터셋 수치를 판정 기준으로 쓰지 않음.

**학습·평가는 출처 단위 분리** — 같은 영상에서 소재를 오려 학습하고 같은 영상으로
채점하면 아무것도 증명되지 않음.

**한 회차에 한 변수만 변경.**

**불리한 수치도 함께 공개** — 실패한 회차와 틀린 진단을 지우지 않음.

**숫자만 보고 판단하지 않음** — 앞선 두 저장소에서 판정이 뒤집힌 경우가 모두 이미지를
열어봤을 때였음.

**한계를 적어 두는 것과 그 기록을 다음 작업에서 쓰는 것은 다름** — 자료 선별을
자동화하려고 주황 화소 비율로 화염을 걸렀다가 kfire03 의 발연 13초를 통째로 놓쳤음.
**같은 실패가 앞선 저장소 `docs/PREREGISTER_F.md` 에 빨간 소방차까지 그대로 적혀 있었고 그 문서를
며칠 전에 읽고 인용까지 한 뒤였음.** 기록은 읽는 것이 아니라 작업 절차에 넣어야 함.

### 이번에 새로 세운 것

**잴 수 있는 것은 잰다. 재지 않고 눈대중하지 않는다.**
이 저장소는 숫자가 결론을 만드는 기록임. 잘못 센 숫자 하나가 그대로 판단으로 굳음 —
실제로 `뽑힘 364장` 이 표에 측정값처럼 적혀 있었고(구간 길이에서 암산한 예상값이었음),
그것을 믿고 추출이 틀렸다고 진단해 재실행까지 요청했음. **실제 값은 350장이었고
추출은 처음부터 맞았음.** 눈대중 한 번이 잘못된 진단을 낳고 잘못된 진단이 사람의
시간을 씀.

**사람이 재생해서 판정하고 기계는 뽑기만 함.**
프레임만으로는 알 수 없는 것이 셋 있음 — **소리, 시간의 흐름, 조리의 맥락**임.
재료를 넣은 것인지 뺀 것인지, 물을 부은 것인지 뚜껑을 덮은 것인지는 재생해야 앎.
**단일 프레임 한계가 확인된 뒤로는 이것이 선택이 아니라 필수가 됨.**

**추천은 추천으로 적고 동의를 받기 전에는 확정으로 기록하지 않음.**
실제로 배경 오탐용 자료 하나를 추천했다가 질문 하나로 철회한 일이 있었음 —
추천으로만 적어 두었기에 되돌리는 데 비용이 들지 않았음.

**원인을 말할 때도 잰다. 패턴이 맞아떨어지는 것은 근거가 아님.**
김 추출에서 8개 구간의 끝 프레임이 안 나왔고 `구간 끝 == 영상 길이` 인 자리에서만
그런 것이 8건, 그 밖에서 0건이었음. 패턴이 깨끗했으나 거기서 멈추고 영상 길이를
실측했더니 **다섯만 그 이유였고 셋은 영상이 충분한데도 안 나왔음.**
패턴만 믿었으면 틀린 원인을 기록으로 굳혔을 것임.

### 시험 합성을 네 번 돌리며 더 세운 것 (2026-08-13)

**검산은 실패할 수 있어야 검산임.**
2회차 시험의 검산 셋이 `자기 자신과의 차 0` · `판 되살림 오차 0` · `알파0 이 나머지를
포함` 이었음. 셋 다 `통과` 를 찍었으나 **어떤 입력에서도 실패할 수 없는 항등식**이었음.
지금은 손으로 답을 셀 수 있는 자료를 만들어 예상값과 대조하고, 상수 알파를 얹어
`알파 × (255 − 배경)` 과 맞는지 봄.

**필요조건을 충분조건처럼 쓰지 않는다.**
`잡음 바닥보다 작으면 구별 못 함` 은 맞음. `크면 보인다` 는 **아님.**
그 둘을 섞어 쓴 탓에 m3 51장이 통과했고 그림에는 아무것도 없었음.

**앞선 저장소의 조치를 옮기기 전에 우리 자료에서 잰다.**
`흰 연기는 흰 급식실에서 안 보인다` 는 원문을 읽고 우리 방식을 실패로 단정했으나,
재 보니 우리 배경에서는 **세 장어치**였고 검댕으로 바꾸면 오히려 얼룩으로 보였음.
**원문을 읽는 것과 우리 자료에서 재는 것 사이를 건너뛰지 않음.**

**남의 원문은 먼저 읽는다.**
`가시성 검사` 를 세 번 고쳐 도달했는데 **앞선 저장소 사전 등록에 이미 있던 규칙**이었음.
네 번째 시험이 끝난 뒤에야 읽었음.

**결과가 갈릴 수 있는 기준은 어느 쪽이 이기는지도 미리 정한다.**
틴트 판정에서 기준을 둘(숫자·눈) 세워 놓고 **어긋났을 때의 순서를 안 정했음.**
결과를 보고 눈을 택했음 — 판정 근거는 그림에 남으나 절차는 흠임.

**줄여 그린 그림으로 판정하지 않는다.**
223화소짜리 라벨 상자를 화면 전체 시트에서 60화소로 보고 판정하려 했음.
지금은 상자만 잘라 원본 배율로 보고, `배경 · 합성 · 차이` 세 칸으로 냄.

---

## 한계

### 착수 시점에 이미 알던 것

**초기 유증기가 김과 색으로 구별되지 않음** — 문제의 본질이며, 조기 경보를 목표로 하는
한 피할 수 없음.

**소방 재현 영상은 급식실이 아님** — 실험장·야외이며 조명과 배경이 다름.

**급식실 김 자료는 있으나 급식실 연기 자료는 없음** — 평가군의 positive 와 negative 가
서로 다른 촬영 조건에서 옴. 급식실에서 불을 내는 실험 영상은 존재하지 않으므로
**자료를 더 모아도 풀리지 않음.**

이 실험이 답할 수 있는 물음은 `소방 재현 영상의 주방 유류화재 발연을 얼마나 잡는가`
까지임. **`급식실에서 얼마나 잡는가` 는 이 자료로 답할 수 없음.**
앞선 저장소가 A-smoke 22.0% 를 두고 쓴 표현을 그대로 빌리면 —
**전이 여부의 증거이지 배치 성능의 근거가 아님.**

### 자료를 모으면서 알게 된 것

**발연점과 눈에 보이는 발연이 다름** — 발연점은 200\~230도인데 계측 영상에서 카메라에
연기가 잡히기 시작하는 것은 **330도 부근**임. **이 실험이 잡을 수 있는 것은 발연점
직후가 아니라 한참 뒤이고 예고 시간도 그만큼 짧아짐.**

**기름이 적으면 예고 시간이 더 짧아짐** — 500mL 는 12분 만에 발화했고 100mL 는 그보다
짧았음. 그런데 기름이 적으면 연기의 양도 적음. **적은 기름과 밝은 배경이 겹치면 빨리
붙는데 보이지 않음.**

**튀김솥 위에서는 발생 위치가 판단 근거가 되지 못함** — 정상 튀김 조리 중에도
유증기가 화면을 뒤덮음.

**평가군에 배경 지름길이 있음** — 양성은 시연장, 음성은 급식실이라 배경만 봐도 점수가
나옴. 시연장 수증기를 음성에 섞어 완화하나 없애지는 못함.

**김 자료의 밝기가 한쪽으로 쏠려 있음** — 어두운 급식실이 사실상 없음.
결과가 밝기 때문인지 김과 연기의 차이 때문인지 갈라낼 수 없음.

**되찾지 못한 자료가 있음** — 앞선 저장소에 kfire05·06·07 의 출처가 설명만 있고 링크가
없어 원본을 회수하지 못했음. **앞으로 받는 자료는 원본 링크를 함께 기록함.**

### 프레임을 뽑고 나서 알게 된 것 (2026-08-11 추가)

**단일 프레임으로는 사람도 김을 구별하지 못함** — 이 문서
[맨 앞](#학습-전에-나온-가장-큰-결과--단일-프레임으로는-사람도-구별-불가)에 적은 것이며
**가장 큰 한계임.** 모델에 요구할 수 있는 한계가 여기서 정해짐.
증거 그림 → [`docs/single_frame_limit.jpg`](docs/single_frame_limit.jpg)

**출처 하나가 하한선과 정확히 같음** — `Let's Chat` 이 서로 다른 프레임 10장이고
출처당 하한선이 10장임. 한 장이라도 적었으면 배정을 다시 짜야 했음.

**분할마다 평가군 크기가 4.4배 다름** — 시드 2 는 s2 하나가 82% 를 차지하고
시드 5 는 옅음 프레임이 0장임. 출처 macro 로 완화하나 없애지는 못함.

**12번은 성능이 한 번도 채점되지 않음** — 다섯 분할 어디에서도 평가군에 안 들어감.

**배경 오탐군이 사실상 개원중 CCTV 하나임** — 그런데 개원중은 김도 연기도 안 담기는
카메라임. 거기서 오탐이 안 나는 것은 배치 환경의 증거가 못 됨.

**연기 박스를 그릴 수 없음** — 같은 프레임을 두 가지로 읽으면 IoU 0.221 로 갈림.
위쪽 경계가 없기 때문임. 실사 박스를 학습에 섞으려던 계획을 철회함.

**자동 판정이 반복해서 실패했음** — 주황 화소 게이트, 화염 절대 임계값(세 번),
워터마크 자동 측정(네 번 연속), 연기 기둥 밝기차. 마지막 것은 기준으로 삼은 벽이
그늘져 있어 **연기가 아니라 조명을 재고 있었음.**
**숫자가 나왔다고 잰 것이 아님.** → [자료원 기록](docs/SOURCES.md) 의 「자동 판정의 실패」

### 소재를 만들고 나서 알게 된 것 (2026-08-13 추가)

**소재가 67장뿐임.** 앞선 저장소는 197종으로 연기 인식 22.0%(부분 성립)를 냈고,
그쪽 사전 등록이 `낮게 나오면 첫 대응은 알고리즘이 아니라 소재 확대` 라고 미리 적어
두었음. **우리는 그 3분의 1이고 출처가 다섯뿐임.**

**분할 5번이 한 출처 15장임** — m3 가 전멸해 kfire03 만 남음. 476장을 만들면 소재당
31.7회 재사용이고 앞선 저장소 7.1회의 4.5배임. **그 분할 결과는 그만큼 덜 믿을 값임.**

**학습 음성이 0장임.** 1층이 세 평가군을 전부 `학습 미사용` 으로 못 박아 쓸 수 있는
급식실 음성이 없음. 앞선 저장소는 negative 를 급식실 5곳 → 8곳으로 늘려 주방 오탐을
88% → 28% 로 낮췄고 `negative 다양성이 오탐을 지배함` 이라 적었음.
**김 오탐이 높게 나오면 원인이 `안 갈린다` 인지 `아무거나 울린다` 인지 못 가름.**

**라벨 상자가 연기보다 넓음** — 질량99 의 채움이 0.049 임. 큰 쪽에서는 상자가 솥
전체를 덮고 연기가 한쪽 끝에만 있는 장이 나옴.

**배경이 알파에 새는 소재가 있음** — 덩어리비가 07 은 0.478\~0.511, j04 는
0.754\~0.830 임. 피복률 상한이 잡는 것은 67장 중 1장뿐임. **오려내기 절이 1층이라
이 회차에서 못 고침.**

**연기색이 정말 255 인지 못 잼** — 판이 알파 계산에 흡수돼 소재만으로는 못 되돌림.
**가정으로 남김.**

**모양을 숫자로 안 잼** — 잡음(m3)과 옅은 참 연기(p2)를 가른 것은 결국 **그림의
모양**이었음. 봉우리도 평균도 단독으로는 못 갈랐음.

---

### 1회차를 돌리고 나서 알게 된 것 (2026-08-14 추가)

**장 단위 채점이 이 모델에서 뜻을 잃음.** 1층이 `장 단위의 맹점은 그대로 남음 — 어디를
봤는지 모름` 이라고 미리 적었는데, 그 맹점이 예상보다 컸음. 정탐의 절반이 넘는 수가
연기가 아닌 곳에서 나왔음. **2회차는 상자 위치를 채점하는 지표가 필요함.**

**입력 기하가 사전 등록에서 비어 있었음.** `imgsz 640` 만 정하고 짧은 변을 어떻게
채울지를 안 정했음. 그 한 칸으로 다섯 중 둘의 판정이 뒤집힘. **사전 등록이 촘촘하다고
믿었으나 실제로는 판정을 뒤집는 자유도가 남아 있었음.**

**출처만으로도 모델만으로도 설명이 안 됨.** 출처별 정탐률이 0.000 과 1.000 으로
갈리고(24개 중 12개가 양극단) 1층이 층화를 안 한 이유로 던진 물음에 `뒤집힌다` 는
답이 나왔음. 다만 **같은 출처가 분할에 따라 0.000 \~ 1.000 으로 갈림**(p2 · 05 · j01).
분산을 갈라 보면 **출처 간 45.9% · 출처 안 54.1%** 로, 어느 모델이 재느냐가 더 큼.
(처음에 `출처가 곧 결과임` 이라 적었으나 재어 보니 과한 진술이었음. 2026-08-14 정정)

**학습 지표가 반대 방향을 가리킴.** `val: images/train` 이라 외운 정도를 재는 값인데,
그 값이 높은 분할일수록 진짜 연기를 못 잡았음(피어슨 -0.880). **학습 지표로 자료 품질을
판단하면 안 됨.**

**2층 결정 둘이 학습 증강에서 깨졌음.** 2층이 `확대 금지` 와 `한 장에 하나` 를 정했으나
ultralytics 기본값 `scale 0.5`(0.5\~1.5배)와 `mosaic 1.0`(네 장 이어 붙임)이 그것을
되돌렸음. **2층을 닫을 때 학습 증강을 항목으로 세우지 않았음.**

**추론 묶음 구성이 결과를 바꿈.** ultralytics 는 한 묶음 안의 그림 크기가 다르면
letterbox 를 정사각으로 바꿈. 한 사이트(숭곡중)에 4K 와 1080p 가 섞여 있어 32장이
다른 기하로 측정됐음. **되돌려 보니 판별비 차이는 0.0018 로 판정에 영향이 없었으나,
자료를 묶는 방식이 결과를 바꿀 수 있다는 것 자체가 이번에 처음 드러난 것임.**

**비율에 t 구간을 씌워 [0, 1] 밖으로 나감.** 출처가 넷\~다섯뿐이라 구간이 매우 넓고
경계를 넘음. 결과를 본 뒤에 방법을 바꾸는 것은 사후 고르기라 **안 바꿨음.**
2회차 사전 등록에서 구간 방법을 정해 둘 것.

---

## 상세 문서

→ **[자료원 기록](docs/SOURCES.md)** — 발연 15개·김 11곳을 왜 채택하고 왜 뺐는지,
수집 경위와 검색어, 자동 판정이 실패한 여덟 건, 틀린 판정과 정정, 계측 근거

→ **[발연 구간 확정 기록](docs/TIMELINE.md)** — 열다섯 개의 발연·발화 구간을
원본 해상도로 초 단위까지 판정한 전수 기록. **프레임 추출이 이 문서에 의존함.**

→ **[김 구간 기록](docs/STEAM_TIMELINE.md)** — 급식실 11곳의 김 구간, 태그와 밀도,
영상 길이 실측, 추출 결과. 아직 확정이 아닌 줄은 `[애매함]` 으로 표시해 둠

→ **[사전 등록](docs/PREREGISTER_S1.md)** — 1층(배정·자료·평가군·채점·보고 규칙)은
소재를 한 장도 만들기 전에, 2층(합성의 세부)은 학습을 돌리기 전에 닫음.
**못 지킨 것과 한계를 따로 절로 뺐음.**

→ **[마무리 판정 기록](docs/NOTE_finalize_v2.md)** — 합성 뒤 후처리(finalize)를 넣을지
판정한 기록. 1판에서 결함 열둘이 나왔고 넷은 판정을 뒤집는 것이었음. **안 넣기로 함.**

**돌린 스크립트** — `scripts/` 아래. 시험 합성과 그 검산이 회차대로 남아 있음.

**재현 범위** — 공개된 스크립트와 사전 등록으로 절차를 추적할 수 있으나, 원본 영상,
합성 이미지, 다섯 분할의 학습 가중치와 실행 당시의 고정 패키지 버전은 저장소에
포함하지 않았음. 따라서 제3자가 이 저장소만으로 동일 수치를 완전히 재실행할 수 있는
상태는 아님. 스크립트는 실행 시 설치된 `ultralytics` 버전을 출력하도록 되어 있으며,
후속 실험에서는 데이터·가중치의 제공 방식 또는 검증 가능한 해시와 고정 환경을 함께
기록해야 함.

    colab_make_matte.py    오려내기 실행 — 소재 175장
    colab_matte_noise.py   소재 알파의 잡음 (덩어리비·상자99)
    colab_synth_trial.py   1회차 시험 — 순환 설계였음
    colab_synth_trial2.py  2회차 — 검산 셋이 실패 불가였음
    colab_synth_trial3.py  3회차 — 검산을 실패할 수 있게 고침
    colab_m3_check.py      m3 가 왜 전멸했는지 원인을 잼
    colab_size_check.py    연기의 진짜 크기 (질량99 상자 ÷ 원본 화면)
    colab_size_rule.py     크기 규칙 1차 — m3 가 뚫림
    colab_size_rule2.py    크기 규칙 확정 — 눈으로 대조한 조건 아래로 안 내려감
    colab_crop_check.py    상자만 잘라 원본 배율로 (배경·합성·차이)
    colab_tint.py          배경 적응 틴트 — 안 쓰기로 정한 근거
    colab_coverage.py      피복률 — 앞선 저장소 게이트가 우리에게 뜻이 있는가
    colab_finalize_check2.py  마무리(finalize) 를 넣을지 판정 — 안 넣기로 함
    colab_build_s1.py      본 합성 2,380장 · 검산 일곱
    colab_qc_s1.py         합성 QC 시트
    colab_train_s1.py      다섯 분할 학습
    colab_eval_inventory.py   평가 자료 점검
    colab_eval_s1_v2.py    1회차 평가 — 판정에 쓴 것
    colab_eval_look.py     정탐·오탐을 눈으로 확인 (장마다의 신뢰도 저장)
    colab_eval_count.py    정탐 위치 세기 1판 — 규칙 `상자가 연기 위`
    colab_eval_count2.py   2판 — 규칙 `최고 상자의 중심`
    colab_eval_count2b.py  2판 고침 — 한 장씩 추론
    colab_eval_count3.py   3판 — 상자를 지우고 표만. **판정에 쓴 것**
    colab_batchgeom_test.py   묶음 크기가 섞이면 결과가 달라지는지 시험
    colab_geom_sensitivity.py 입력 기하 민감도 + 숭곡중 32장 되돌리기
    colab_macro_ci.py      E·D macro 의 95% 구간 (출처별 값까지 저장)

---

## 관련 저장소

[kitchen-fire-poc](https://github.com/K-H-MOON/kitchen-fire-poc) — 화염 인식 (종결된 기록).
합성 학습 파이프라인, 평가 설계, 탐지 JSON 규격, 웹3D 전이 측정.
**이 저장소가 이어받는 6·7회차 연기 실험의 원 기록.**

[kitchen-person-poc](https://github.com/K-H-MOON/kitchen-person-poc) — 작업 범위 사람 인식 (진행 중).
